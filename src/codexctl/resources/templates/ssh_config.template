# Default SSH config template used by `codexctl ssh-init` when no project-specific
# template is provided. You can copy this file and point project.yml → ssh.config_template
# to your customized version. Supported tokens:
#   {{IDENTITY_FILE}}  → absolute path to the generated private key
#   {{KEY_NAME}}       → key filename (without .pub)
#   {{PROJECT_ID}}     → project id

Host *
  IdentitiesOnly yes
  StrictHostKeyChecking accept-new
  IdentityFile {{IDENTITY_FILE}}

# Common Git host (kept explicit; inherits Host * defaults)
Host github.com
  HostName github.com
  User git
  IdentityFile {{IDENTITY_FILE}}
